name: Release

on:
  push:
    tags:
      - "test*"

# on:
#   push:
#     branches:
#       - gh_action
env:
  CARGO_TERM_COLOR: always

jobs:
  prepare:
    runs-on: ubuntu-latest
    steps:
      - name: Create release --title "${{ github.ref_name }}" --notes "Release ${{ github.ref_name }}"
        run: gh release create "${{ github.ref_name }}" --draft
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  release:
    needs:
      - prepare
    strategy:
      matrix:
        builds:
          - target: x86_64-pc-windows-msvc
            os: windows-latest
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
    runs-on: ${{ matrix.builds.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Build
        run: cargo build --release --target "${{ matrix.builds.target }}"

      - name: Tests
        run: cargo test

      - name: Package
        run: |
          export ARCHIVE_NAME="firework-0.1.0-${{ matrix.builds.target }}.zip"
          echo "::set-env name=ARCHIVE_NAME::$FOO"

          if [ '${{ matrix.builds.os }}' == 'windows-latest' ]; then
            7z a -stl $ARCHIVE_NAME "./target/release/rust-firework-gui-app.exe" "./target/release/rust-firework-cli-app.exe"
            7z rn $ARCHIVE_NAME "./target/release/rust-firework-gui-app.exe" firework.exe
            7z rn $ARCHIVE_NAME "./target/release/rust-firework-cli-app.exe" firework-cli.exe
          else
            7z a -stl $ARCHIVE_NAME "./target/release/rust-firework-gui-app" "./target/release/rust-firework-cli-app" 
            7z rn $ARCHIVE_NAME "./target/release/rust-firework-gui-app" firework
            7z rn $ARCHIVE_NAME "./target/release/rust-firework-cli-app" firework-cli
          fi

          # 7z a $ARCHIVE_NAME "License"

      - name: Upload
        run: |
          gh release upload ${{ github.ref_name }} $ARCHIVE_NAME
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  publish:
    needs:
      - release
    runs-on: ubuntu-latest
    steps:
      - name: Publish release
        run: |
          echo would run "gh release edit ${{ github.ref_name }} --draft=false"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
