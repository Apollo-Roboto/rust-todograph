[package]
name = "tg-gui-app"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = { workspace = true, features = ["serde"] }
log = { workspace = true }
open = "5.3.2"
rfd = { workspace = true }
slint = { version = "1.15.1" }

tg-core = { path = "../core" }

[build-dependencies]
slint-build = "1.15.1"
