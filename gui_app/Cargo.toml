[package]
name = "rust-firework-gui-app"
version = "0.1.0"
edition = "2024"

[dependencies]
slint = "=1.12.1"
chrono = { workspace = true, features = ["serde"] }
rust-firework-core = { path = "../core" }

[build-dependencies]
slint-build = "=1.12.1"
