[package]
name = "rust-firework-gui-app"
version = "0.1.0"
edition = "2024"

[dependencies]
slint = "1.13.1"
chrono = { workspace = true, features = ["serde"] }
rfd = { workspace = true }
rust-firework-core = { path = "../core" }

[build-dependencies]
slint-build = "1.13.1"
