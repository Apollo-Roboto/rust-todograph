
export enum MindTaskState {
    Todo,
    Doing,
    Done,
}

export struct MindTask {
    id: int,
    title: string,
    notes: string,
    state: MindTaskState,
    x: length,
    y: length,
    parent_id: int,
    parent_index: int,
    childrens: [int],
    completion: float,
}
