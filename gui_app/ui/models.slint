
export enum MindTaskState {
    Todo,
    Doing,
    Done,
}

export struct MindTask {
    id: int,
    title: string,
    state: MindTaskState,
    x: length,
    y: length,
    parent: int,
    childrens: [int],
    completion: float,
}
